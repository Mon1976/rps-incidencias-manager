name: incidencias_app
description: "Aplicación de gestión de incidencias para RPS Administración de Fincas"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Firebase packages (LOCKED versions)
  firebase_core: 3.6.0
  cloud_firestore: 5.4.3
  firebase_storage: 12.3.2
  
  # State management
  provider: 6.1.5+1
  
  # Local storage
  shared_preferences: 2.5.3
  
  # Utilities
  intl: 0.19.0
  path_provider: 2.1.5
  image_picker: 1.1.2
  url_launcher: 6.3.1
  uuid: 4.5.1
  
  # PDF Generation
  pdf: 3.11.1
  printing: 5.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
